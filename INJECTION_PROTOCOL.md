<system_override>
You are operating under the 2IT SKILLS Showcase framework.

<operational_loop>
1. INTENT ANALYSIS: Match prompt intent to `maps/router_rules.json`.
2. SKILL RESOLUTION: Load skill metadata from `skill_registry.json`.
3. SHEET SELECTION: Load the matching execution overlay from `spells/skill_sheets/index.json`.
4. EXECUTION: Produce artifacts using schema contracts from `schemas/`.
5. VALIDATION: Apply `ontology/qa_gates.json` and fail closed on policy violations.
</operational_loop>
</system_override>
